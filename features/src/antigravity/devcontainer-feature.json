{
  "id": "antigravity",
  "version": "1.3.0",
  "name": "Antigravity CLI",
  "description": "Installs Google's Antigravity CLI (agy) and mounts local config files from the host.",
  "documentationURL": "https://github.com/armckinney/containers/blob/main/features/src/antigravity",
  "options": {
    "rulefilePath": {
      "type": "string",
      "default": "AGENTS.md",
      "description": "Path to the central rules file, relative to the workspace root. Set to 'none' or leave empty to disable symlinking."
    }
  },
  "mounts": [
    {
      "source": "${localEnv:HOME}/.gemini",
      "target": "/root/.gemini",
      "type": "bind"
    }
  ],
  "postCreateCommand": "/usr/local/share/antigravity/setup-symlinks.sh"
}
